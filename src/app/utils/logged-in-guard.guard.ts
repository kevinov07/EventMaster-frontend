// import { CanActivateFn } from '@angular/router';

// export const loggedInGuardGuard: CanActivateFn = (route, state) => {
//   if (this.authService.isLoggedIn()) {
//     // Si el usuario ya ha iniciado sesión, redirige a la página de inicio
//     this.router.navigate(['/dashboard']);
//     return false; // Cancela la navegación actual
//   } else {
//     return true; // Permite la navegación a la página de inicio de sesión
//   }
//   return true;
// };
